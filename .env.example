# Example env file for Vite. Copy to one of the following and adjust values:
# - .env.development.local (used by `npm run dev`)
# - .env.production.local  (used by `npm run build`)

# n8n webhook endpoint
VITE_N8N_WEBHOOK_URL=https://n8n.example.com/webhook/<workflow-id>

# Optional shared secret header (if validated in n8n)
VITE_N8N_WEBHOOK_SECRET=change-me

# reCAPTCHA site key (if using the contact form)
VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# Supabase configuration
VITE_SUPABASE_URL=https://your-supabase-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key