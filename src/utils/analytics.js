export function trackPageview(path) {
  // Placeholder: wire to your analytics tool of choice
  // Example: window.gtag?.('event', 'page_view', { page_path: path });
  // console.debug('[analytics] pageview:', path);
}
